import { Text } from "react-native";
import { ScreenContainer } from "../components/ScreenContainer";

export function AskScreen() {
  return (
    <ScreenContainer title="Ask">
      <Text style={{ color: "#8b97b0" }}>Ask coming up.</Text>
    </ScreenContainer>
  );
}
